import { createGlobalStyle } from 'styled-components';


const Colors = createGlobalStyle`
:root {
    --primaryDark: #000000;
    --primaryLight: #ffffff;
    --primaryAccent: #009978;
}
`;

export default Colors;